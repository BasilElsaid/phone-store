export interface ProductSpecs {
    chipset: string;
    display: string;
    build: string;
    camera: string;
    frontCamera: string;
    battery: string;
    charging: string;
    os: string;
    extras?: string; // optional property
}
