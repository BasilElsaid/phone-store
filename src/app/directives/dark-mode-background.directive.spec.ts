import { DarkModeBackgroundDirective } from './dark-mode-background.directive';

describe('DarkModeBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new DarkModeBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
